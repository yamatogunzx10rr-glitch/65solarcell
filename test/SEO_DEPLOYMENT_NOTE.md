# SEO deployment note

The page now includes optimized title/description tags, Open Graph and Twitter metadata, image alt text, semantic section labels, and LocalBusiness JSON-LD.

Before publishing, replace the placeholder sitemap comment in `robots.txt` with the real absolute URL, create `sitemap.xml` using the production URL, and add a self-referencing canonical link with that same URL. Then verify the deployed page in Google Search Console and the Rich Results Test.
